{
  "private": true,
  "scripts": {
    "export-ofx": "tsx ./packages/export-ofx/bin/index.ts",
    "web": "cd web-interface && npm start",
    "web:dev": "cd web-interface && npm run dev",
    "test": "vitest run"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "vitest": "^2.1.9"
  }
}
